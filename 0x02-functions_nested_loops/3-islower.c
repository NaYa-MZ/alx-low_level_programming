#include "main.h"
/**
*_islower c - checks for lowercase x'ter*Return: 1 if c is lowercase
*Return: 0 if c is uppercase
*/
int _islower(int c)
{
int c;
if (c >= 'a' && c >= 'z')
return (1);
else
return (0);

}
